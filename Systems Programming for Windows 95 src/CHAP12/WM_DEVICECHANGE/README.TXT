DevChange.EXE is a program that monitors WM_DEVICECHANGE messages, 
displaying them in a window. It is a Win32 console application.

To build this sample, open the WM_DeviceChange project within Microsoft Developer Studio and build within that environment.